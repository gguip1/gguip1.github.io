---
title: '브라우저는 3초, curl은 40ms였다.'
slug: '3'
kicker: 'CASE No. 03'
date: '2025-09-18'
metric:
  before: '쿼리 369ms · scan 1.8M'
  after: '쿼리 16ms · scan 32'
  delta: '23× 개선 · 복합 인덱스 적중'
related: ['1', '2']
tags: ['OpenTelemetry', 'Tempo', 'MongoDB', '인덱스 튜닝']
readingMinutes: 7
---

같은 요청이었다. 브라우저에선 **1.7~3.5초**. `curl` 로 같은 엔드포인트를 치면 **38~58ms**.
이 모순을 가설로 덮는 게 제일 쉽다. "브라우저 렌더링 이슈" · "네트워크 스로틀링" · "CDN 오버헤드" ...
하지만 이 종류의 가설은 **거의 맞지 않는다**. 내 경험상 가설 4~5개 중 원인은 없고, 시간만 하루 날아간다.

## 트레이스로 분해했다

OpenTelemetry + Tempo 로 요청 한 건을 끝까지 펼쳐봤다.

```
request /chat/history/:threadId       total = 1,723ms
├─ auth.verify                           12ms
├─ connection.acquire                 1,680ms  ← !
├─ db.query (MongoDB)                    12ms
├─ response.serialize                     6ms
└─ ...
```

눈에 들어온 건 `connection.acquire` 이 **1,680ms** 를 차지하는데, 정작 `db.query` 는 **12ms** 밖에 안 된다는 모순.
`curl` 은 keep-alive 로 이미 연결이 살아 있기 때문에 `connection.acquire` 가 0에 가깝다 — 그래서 40ms.
브라우저는 매 페이지 이동마다 새 연결을 맺는데, 그 **connect 시간만 1.7초 걸리고 있었다**.

## 왜 connect 가 느린가

여기서 함정 한 겹이 더 있었다. 겉보기에는 "연결을 새로 맺는 데 오래 걸린다" 로 읽히지만, 풀 자체는 살아 있었다.
진짜 문제는 **풀 안의 연결이 오래 점유되어서, 새로 요청이 들어왔을 때 가용 커넥션이 없었다**는 쪽이었다.

질문은 한 단계 더 올라간다. **왜 기존 커넥션이 오래 점유되고 있었나?**

## 진짜 원인은 인덱스였다

실행 계획을 들여다봤다.

```
db.messages.find({ meetingId: X, createdAt: { $gte: Y } }).sort({ createdAt: -1 })
        → scanned: 1,820,445 docs
        → returned: 32 docs
        → time: 369ms
```

`meetingId` 필터는 있었지만, 거기에 붙은 `createdAt` 정렬이 **기존 인덱스를 탈 수 없는 형태**였다.
컬렉션을 거의 전수 스캔하는 쿼리가 평균 수백 ms 씩 커넥션을 점유하면서, 뒤따라 들어온 요청들의 `connection.acquire` 까지 지연시키고 있었다.

타깃은 복합 인덱스.

```js
db.messages.createIndex({ meetingId: 1, createdAt: -1 })
```

## 결과

```
db.messages.find({ meetingId: X, createdAt: { $gte: Y } }).sort({ createdAt: -1 })
        → scanned: 32 docs     (-99.998%)
        → returned: 32 docs
        → time: 16ms            (23× faster)
```

- 쿼리: **369ms → 16ms** (**23×**)
- connect 지연: 트레이스에서 사라짐. 풀이 비지 않으니 새 연결을 맺을 필요가 없음.
- 체감 페이지 로딩: **1.7초 → 0.18초**.

## 배운 것

1. **모순된 측정치는 선물이다.** 브라우저 3 초 vs curl 40 ms 는 답을 숨기고 있는 게 아니라 손짓하고 있었다.
2. **트레이스의 depth 를 끝까지 본다.** `connection.acquire` 가 느리다는 사실이 드라이버 문제가 아니라, **다른 쿼리가 풀을 점유하고 있었다**는 단서였다.
3. **증상과 원인은 여러 단계 떨어져 있다.** "페이지 느림 → 연결 획득 느림 → 쿼리 느림 → 인덱스 없음." 직관으로 이 단계들을 한 번에 건너뛰는 건 불가능하다.
