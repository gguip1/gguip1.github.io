# Portal harnesses

Add a folder per verification purpose only when its contract is known.

Examples may include `browser/`, `visual/`, or `catalog/`. Every harness folder must contain a `HARNESS.md`.
